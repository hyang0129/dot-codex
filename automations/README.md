# Automations

Put automation definitions you want to keep in source control here.

Expected layout:

```text
automations/
  <automation-id>/
    automation.toml
    memory.md
```

Only version automations that are intentionally shared. Runtime scheduling state lives elsewhere and should not be tracked.
