# Local Plugins

Put source-controlled local Codex plugins here.

Suggested layout:

```text
plugins/
  my-plugin/
    .codex-plugin/
      plugin.json
    skills/
    agents/
    assets/
```

This directory is meant for plugins you own or intentionally vendor. Marketplace cache or temporary plugin downloads should stay out of this repo.
